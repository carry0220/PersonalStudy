package come.carry.personalstudy.twenty.april.day19.code;

//如果if中包含return的话可以使用提前return的方法来终止if-else
public class one{
  public static void main(String[] args) {
      String name = "carry";
      if (!"carry".equals(name))
          return;
  }
}